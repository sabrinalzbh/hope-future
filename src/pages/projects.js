import * as React from 'react'
import Layout from '../components/layout'

const ProjectPage = () => {
    return (
        <Layout pageTitle="Projects">

            <p>Here, you will find my latest work. I don't have much to show just yet, but it's in the making. Stay tuned!</p>
            </Layout>
    )
}

export default ProjectPage 